import React from 'react';
import ReactDOM from 'react-dom/client';
import GWMDashboard from './GWMDashboard';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<GWMDashboard />);
